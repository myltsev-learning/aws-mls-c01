In machine learning, classes are the distinct categories or labels that a model is trained to predict.

**Examples:** "Spam"/"Not spam" (email); "Dog"/"Cat"/"Bird" (images); "Will churn"/"Will not churn" (customers).

#card 